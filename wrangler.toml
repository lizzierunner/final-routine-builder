# Cloudflare Workers Configuration
# This file configures your Cloudflare Worker deployment

name = "loreal-routine-builder"
main = "worker.js"
compatibility_date = "2024-01-01"

# Environment variables (don't put actual API key here)
# Set the OPENAI_API_KEY in Cloudflare dashboard:
# Workers & Pages → Your Worker → Settings → Variables → Environment Variables

# [vars]
# Add any non-secret variables here

# To deploy:
# 1. Install Wrangler CLI: npm install -g wrangler
# 2. Login to Cloudflare: wrangler login
# 3. Set your API key as secret: wrangler secret put OPENAI_API_KEY
# 4. Deploy: wrangler deploy
